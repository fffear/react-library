import React from "react";

const header = () => {
  return (
    <header className="App-header">
      <h1>My Library</h1>
      <button>Add New Book</button>
    </header>
  );
};

export default header;
